# 환경 변수 (API 키, 관리자 PIN 등 비밀값)
.env
# env-config.js 는 기본값(빈 객체)을 커밋 — API 키 추가 시엔 gitignore 처리할 것

# 환경별 경로 설정 (개인 경로 포함 — 커밋 금지)
ENV.md

# macOS
.DS_Store
**/.DS_Store

# Claude Code 내부 설정
.claude/

# 에디터
.vscode/
.idea/

# Node
node_modules/

# 사용자 세션 스킬 (로컬 전용)
skills/session/
