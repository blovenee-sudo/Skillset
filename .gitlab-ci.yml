pages:
  stage: deploy
  script:
    - mkdir -p public/skills
    - cp index.html ux_skillset_v10.html manifest.json sw.js icon.svg env-config.js public/
    - cp skills/*.json skills/*.md public/skills/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
