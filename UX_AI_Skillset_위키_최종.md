# UX AI 스킬셋 — 개발 과정 기록
> 작성일: 2026년 4월  
> 작성자: 김보니 · 서비스UX디자인실  
> 앱 접속: `ux_skillset_v10.html` (사내 서버 배포 후 URL로 대체)

---

## 배경 — 왜 만들었나

2026 GPR 목표 설정 과정에서 UX 기획 업무 전반에 AI를 도입하기 위한 스킬셋 9종을 정리하게 됐다. 스킬셋을 단순히 문서로 보관하는 것에 그치지 않고 **실제 업무 중 즉시 꺼내 쓸 수 있는 도구**로 만들기 위해 인터랙티브 앱 형태로 제작하기로 했다.

> "위키 페이지는 보관에 강하지만 실사용에 약하다. 스킬셋은 매일 꺼내 쓰는 도구이므로 앱이 맞다. 위키는 기록·히스토리 용도로 역할을 분리한다."

---

## 핵심 스펙 — 무엇을 만들었나

### 1. 스킬 라이브러리 + 프로젝트 중심 구조

초기 버전은 스킬을 필터로 분류해서 클릭하는 단순 런처였다. 그러나 실제 업무에서는 **프로젝트 단위로 스킬을 묶어서 쓰는 방식**이 더 자연스럽다는 판단에 따라 구조를 전면 전환했다.

- 왼쪽 사이드바에 스킬 라이브러리를 두고
- 오른쪽 프로젝트 영역으로 드래그 앤 드롭해서 스킬을 배치
- **같은 스킬을 여러 프로젝트에 중복 배치 가능** (스킬 인스턴스 개념)
- 프로젝트는 자유롭게 생성, 트랙 구분 없음

### 2. 슬래시 명령어 (Claude Code / Cursor 연동)

각 스킬에 `/spec-analysis`, `/figma-draft` 같은 슬래시 명령어를 부여했다. Claude Code 또는 Cursor에서 `/명령어`를 입력하면 해당 프롬프트를 바로 호출할 수 있는 형태로 설계했다. 사내 깃 서버 배포 후 `.cursorrules`에 스킬 목록을 등록하면 자동완성도 가능하다.

### 3. 권한 구조

팀 공유를 고려해 역할별 권한을 분리했다.

| 권한 | 가능한 작업 |
|------|------------|
| 비로그인 | 스킬 조회, 프롬프트 복사 |
| 일반 사용자 | 스킬 추가, 본인 작성 스킬 편집 |
| 관리자 (PIN) | 전체 스킬 편집·삭제 |

### 4. 사용 기록 + ER 달성 추적

스킬을 실제로 사용할 때마다 "사용 +(누적카운트)" 버튼을 눌러 기록한다. 상단 바의 "사례 N/10건" 카운터가 실시간으로 업데이트되며, GPR ER2(AI 활용 사례 10건 이상) 달성 여부를 추적하는 수단이 된다. 자동 카운트가 아닌 수동 기록인 이유는 "프롬프트를 복사만 하고 실제로 안 쓴 경우"를 걸러내기 위해서다.

### 5. 즐겨찾기 + 사이드바 탭 구조

자주 쓰는 스킬을 빠르게 접근하기 위해 즐겨찾기 기능을 추가했다. 사이드바는 세 개의 탭으로 구성된다.

- **스킬 탭**: 전체 스킬 목록 (즐겨찾기 상단 정렬)
- **즐겨찾기 탭**: ★ 표시된 스킬만 모아보기
- **프로젝트 탭**: 프로젝트 목록, 클릭 시 해당 카드로 스크롤

각 탭에는 독립적인 검색이 적용된다 (탭 전환 시 검색어 초기화).

### 6. 설정값(.env)과 다중 PC

개인 PC와 회사 PC 등 **환경마다 다른 설정**(관리자 PIN, 향후 API Key 등)을 쓰기 위해, 브라우저가 직접 `.env`를 읽는 대신 **`node scripts/apply-env.mjs`** 로 `.env`를 읽어 `env-config.js`를 생성하고, 앱은 그 파일을 통해 `window.__UX_SKILLSET_ENV__`로 값을 참조한다.

- 각 PC의 `skillset` 폴더에 `.env.example`을 복사해 `.env`로 두고 값을 채운 뒤 `apply-env` 실행.
- `.env`는 Git에 올리지 않는다(`.gitignore`). `env-config.js`에 API Key 등을 넣은 상태로는 **공개 저장소에 커밋하지 않는다**(브라우저에서 노출됨).

### 7. 기획 산출 형식 — 마크다운 기본

어떤 기획안·정리 산출이든 **기본 출력 형식은 마크다운(GFM)**으로 통일한다. 스킬 앱에서 「프롬프트 복사」 시 LLM에 붙여넣을 내용 끝에 **공통 마크다운 출력 규칙**이 자동으로 덧붙도록 했다(에디터에 보이는 본문과 클립보드가 다를 수 있음).

---

## 포함된 스킬 목록

| 번호 | 스킬명 | 슬래시 명령어 | 설명 |
|------|--------|--------------|------|
| SKILL 00 | 피그마 초안 자동 생성 | `/figma-draft` | CaMP 설계킷 기반 화면 초안 |
| SKILL 01 | 스펙 분석 · 문의 목록 | `/spec-analysis` | 스펙 파악 + 애매한 항목 추출 |
| SKILL 02 | 벤치마킹 · 인사이트 | `/benchmarking` | 유사 서비스 사례 수집 |
| SKILL 03 | As-is / To-be 정리 | `/asis-tobe` | 설득 자료용 비교표 |
| SKILL 04 | 플로우차트 · 프로세스 맵 | `/flowchart` | Mermaid 다이어그램 자동 생성 |
| SKILL 05 | 기능 정의서 · 엣지케이스 | `/feature-spec` | 세부 기능 + 예외 처리 정리 |
| SKILL 06 | 휴리스틱 평가 1차 탐지 | `/heuristic` | 닐슨 10원칙 기반 화면 점검 |
| SKILL 07 | 회의록 · 액션아이템 | `/meeting-notes` | 미팅 후 정리 자동화 |
| SKILL 08 | 리뷰 코멘트 수정 목록 | `/review-comments` | 피그마 리뷰 코멘트 우선순위화 |

---

## 개발 과정 요약

| 단계 | 주요 변경 내용 |
|------|--------------|
| 1차 | 클릭 런처 형태로 시작. 스킬 카드 클릭 → 프롬프트 복사. Track A/B 필터 구조 |
| 2차 | **프로젝트 중심 구조로 전면 전환.** 드래그 앤 드롭, 중복 배치, 슬래시 명령어, 권한 구조 추가 |
| 3차 | 프로젝트 내 스킬 순서 드래그 변경. 상세 패널을 3열 우측으로 분리 |
| 4차 | **2열 레이아웃 + 하단 슬라이드업 상세 패널로 재설계.** 사이드바 탭·검색 추가 |
| 5차 | 섹션 구분·타입 태그 제거. 빈 공간·배경색 통합으로 시각적 일체감 개선 |
| 6차 | 온디맨드 → **즐겨찾기** 기능으로 대체. 사용+1 버튼 버그(id 0 falsy) 수정. 신규 스킬 최상단 배치 |
| 7차 | **즐겨찾기 탭 추가.** 버튼명 `사용 +(누적카운트)` 로 변경 |
| 8차 | **`.env` → `env-config.js` 경유 설정**, 복사 시 **마크다운 공통 규칙** 자동 첨부 |

---

## 사용 방법

```
0. (최초·PC 옮길 때) .env.example → .env 복사, 값 입력 후 skillset 폴더에서 `node scripts/apply-env.mjs` 실행
1. 사이드바 스킬 탭에서 스킬 카드 클릭
2. 하단 상세 패널에서 프롬프트 확인 → 복사하기 (마크다운 공통 규칙이 클립보드에 자동 첨부됨)
3. Claude 등 LLM에 붙여넣어서 실제 사용
4. 프로젝트에 스킬 드래그 → 해당 프로젝트 카드에 배치
5. 사용 후 "사용 +N" 버튼 클릭 → 사례 카운터 누적
```

---

## 배포 현황

| 항목 | 내용 |
|------|------|
| 파일 | `ux_skillset_v10.html` + (선택) `env-config.js` |
| 설정 | `.env` 작성 후 `node scripts/apply-env.mjs` → `env-config.js` 생성. `ADMIN_PIN` 등은 `.env`에서 주입 가능 |
| 데이터 저장 | 브라우저 localStorage (개인별 독립 저장) |
| 로컬 실행 | `apply-env` 실행 후 HTML을 브라우저로 열고 북마크 (`env-config.js` 없으면 기본 PIN 등만 사용) |
| 팀 공유 | 사내 깃 서버 push → GitLab Pages URL 배포 (cursor_deploy_guide.md 참고) |
| 관리자 PIN | `.env`의 `ADMIN_PIN` 또는 앱 기본값(비밀 커밋 금지) |
